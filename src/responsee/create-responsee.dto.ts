export class CreateResponseeDto {
    query: string;       // La question posée par l'utilisateur
    response: string;    // La réponse générée par l'IA
    category: string;    // La catégorie de la question (pregnancy, baby, general)
  }
  